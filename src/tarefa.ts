function multiplicar(a: number, b: number): number {
    return a * b
}

const resultado: number = multiplicar(10, 10);
console.log( resultado);


function saudacao(nome:string): string {
    return "Olá" + nome;
}

const ola: string = saudacao("Matheus");
console.log( ola);
